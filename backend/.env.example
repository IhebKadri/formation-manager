# Server Configuration
SERVER_PORT=8080

# PostgreSQL Configuration
DB_URL=jdbc:postgresql://
DB_USERNAME=
DB_PASSWORD=

# JWT Secrets
# (Make sure to keep this private and never commit it)
JWT_SECRET=
JWT_ACCESS_EXPIRATION=
JWT_REFRESH_EXPIRATION=
